import Landing from "@/pages/Landing";

// Waiting page wrapper.
// For now it reuses the current landing experience (hero + countdown + waitlist).
export default function Waiting() {
    return <Landing />;
}

